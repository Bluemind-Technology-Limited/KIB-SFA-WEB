# API base URL for the Sales Force Automation backend.
# The admin frontend runs against simulated data until the API is available.
# VITE_API_URL="/api"

# How long simulated requests wait before resolving (milliseconds).
# VITE_MOCK_LATENCY=450
